# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets (commit .env.example only)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code: ignore everything personal, but share the project settings (hooks etc.)
.claude/*
!.claude/settings.json

# Instagram session (contains auth cookies — never commit)
.ig-session.json

# Playwright MCP artifacts
.playwright-mcp/

# Session memory
.remember/
.ijfw/
.superpowers/
.worktrees/

# Local audit/tagging artifacts
audit-page*.png
tags-batch*.jpeg

# Curated artist reference images — third-party portfolio work kept local only,
# never redistributed in the public repo. The app falls back to monogram
# placeholders (see src/components/ArtistImage.jsx) when these are absent.
public/images/artists/
