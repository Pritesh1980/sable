{
  "name": "tattoo-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "docs:check": "node --test scripts/checkMermaidDocsTest.js && node scripts/checkMermaidDocs.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "preview": "vite preview",
    "audit:targets": "node scripts/auditTouchTargets.mjs",
    "deploy:aws": "node scripts/deployAws.mjs"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "@huggingface/transformers": "^4.2.0",
    "@supabase/supabase-js": "^2.112.4",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-router": "^8.3.1",
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@tailwindcss/postcss": "^4.3.3",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.3",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^6.1.1",
    "eslint": "^10.9.1",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.5",
    "fake-indexeddb": "^6.2.5",
    "globals": "^17.11.0",
    "jsdom": "^29.1.1",
    "mermaid": "11.17.2",
    "playwright": "^1.62.1",
    "postcss": "^8.5.26",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "_overridesNote": "Transitive security bumps with no upstream release yet. Drop each entry once its parent ships a fixed range. adm-zip: GHSA-vwc7-r8mq-g2x9 (symlink-following extraction, arbitrary file overwrite), reached only via @huggingface/transformers > onnxruntime-node (the Node runtime, never bundled or executed here — the Taste Engine is browser-only). @babel/core: sourceMappingURL arbitrary file read, dev-only via eslint-plugin-react-hooks; pinned to the 7.x patch, not 8.x. sharp: libvips/libheif CVEs, transitive via @huggingface/transformers — same browser-only Taste Engine path, never executed here; bumped to the latest even though the libheif advisory has no clean fix yet. browserslist: normalizeStats prototype write via untrusted stats json, build-tooling only. @humanfs/node: symlink-following recursive copy, dev-only via eslint. dompurify: detached-subtree XSS, transitive via mermaid (docs tooling). nanoid: zero-size infinite loop, transitive build dep.",
  "overrides": {
    "adm-zip": "^0.6.1",
    "@babel/core": "^7.29.6",
    "sharp": "^0.35.0",
    "browserslist": "^4.28.9",
    "@humanfs/node": "^0.16.8",
    "dompurify": "^3.4.13",
    "nanoid": "^3.3.18"
  }
}
